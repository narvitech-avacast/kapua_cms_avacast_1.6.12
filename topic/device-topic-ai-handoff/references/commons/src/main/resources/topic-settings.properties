# Sanitized reference only. Fill values from the target environment.
broker.scheme=tcp
broker.host=<BROKER_HOST>
broker.port=1883

# Kapua/Kura control topic prefix used by Topic.java and Broker ACL rules.
commons.control_message.classifier=$EDC
