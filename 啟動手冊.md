# Kapua CMS 1.6.12 啟動手冊

## 架構總覽

| 容器 | 映像 | 說明 | 對外 Port |
|------|------|------|-----------|
| `nginx` | nginx:alpine | 反向代理，統一入口 | 80, 443, 8443 |
| `kapua-console` | kapua/kapua-console:1.6.12 | Web 管理介面 | 8083, 8445 |
| `kapua-api` | kapua/kapua-api:1.6.12 | REST API（自訂 JAR） | 僅內部 |
| `job-engine` | kapua/kapua-job-engine:1.6.12 | 背景排程引擎 | 僅內部 |
| `broker` | kapua/kapua-broker:1.6.12 | MQTT Broker | 1883, 8883, 61614 |
| `db` | kapua/kapua-sql:1.6.12 | 資料庫（H2/MariaDB） | 3307 |
| `es` | elasticsearch:7.8.1 | 資料儲存搜尋引擎 | 9200, 9300 |
| `events-broker` | kapua/kapua-events-broker:1.6.12 | AMQP 事件匯流排 | 5672 |
| `debug-proxy` | compose-debug-proxy（本地 build）| API 偵錯代理 | 9999（UI）, 8081, 8082, 8444 |

**compose 工作目錄：**
```
d:\Kapua_cms_bill_1.6.12\kapua-1.6.12\kapua-1.6.12\deployment\docker\compose
```

---

## 一次性前置設定（第一次或重裝後才需要）

### 1. 設定 hosts 檔（需系統管理員）

用 **系統管理員身份** 開啟 PowerShell，執行：

```powershell
d:\Kapua_cms_bill_1.6.12\tools\add-cms-test-host.ps1
```

這會在 `C:\Windows\System32\drivers\etc\hosts` 加入：
```
172.16.19.37  cms.test
```

> 驗證：`ping cms.test` 應回應 `172.16.19.37`

---

## 每次開機啟動流程

### 步驟 1：確認 Docker Desktop 已啟動

開啟 Docker Desktop，等待狀態顯示為 **Running**（右下角 Docker 圖示變綠色）。

```powershell
# 確認 Docker 正常運作
docker info
```

### 步驟 2：切換到 compose 目錄

```powershell
cd "d:\Kapua_cms_bill_1.6.12\kapua-1.6.12\kapua-1.6.12\deployment\docker\compose"
```

### 步驟 3：啟動所有容器

#### 情境 A：上次用 `stop` 關機（容器仍存在）

```powershell
docker compose start
```

啟動後跳至「步驟 4 確認狀態」。

#### 情境 B：上次用 `down` 關機，或容器已被移除

```powershell
$env:IMAGE_VERSION = "1.6.12"
docker compose up -d
```

> `docker compose up -d` 啟動後，**還需要補部署自訂 JAR**（見步驟 3b）。

#### 步驟 3b：部署自訂 API JAR（情境 B 才需要）

等容器全部啟動後（約 30 秒），將修改過的 REST API JAR 複製進容器：

```powershell
# 複製自訂 JAR 進 kapua-api 容器
docker cp "d:\Kapua_cms_bill_1.6.12\kapua-api-resources-running.jar" `
    kapua-api:/var/opt/jetty/webapps/root/WEB-INF/lib/kapua-rest-api-resources-1.6.12.jar

# 重啟 kapua-api 讓新 JAR 生效
docker restart kapua-api
```

> kapua-api 重啟約需 20–30 秒才會接受請求。

### 步驟 4：確認所有容器都在運行

```powershell
docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"
```

預期看到 9 個容器都是 **Up** 狀態：

```
NAMES            STATUS
debug-proxy      Up ...
kapua-api        Up ...
nginx            Up ...
kapua-console    Up ...
job-engine       Up ...
broker           Up ...
db               Up ...
events-broker    Up ...
es               Up ...
```

---

## 服務存取網址

| 用途 | 網址 |
|------|------|
| **主要入口（HTTPS）** | https://cms.test |
| **Web 管理介面（直連）** | http://localhost:8083 |
| **REST API（HTTPS）** | https://cms.test/api/v1 |
| **REST API（直連）** | https://localhost:8443/api/v1 |
| **API 偵錯代理 UI** | http://localhost:9999 |
| **MQTT Broker** | mqtt://localhost:1883 |
| **資料庫（外部連線）** | localhost:3307 |

> **帳號密碼**：預設帳號 `kapua-sys` / `kapua-password`

---

## 關機流程

### 方法 A：暫停（推薦，下次啟動快）

```powershell
cd "d:\Kapua_cms_bill_1.6.12\kapua-1.6.12\kapua-1.6.12\deployment\docker\compose"
docker compose stop
```

- 容器保留（不刪除），下次用 `docker compose start` 即可
- **不需要重新部署自訂 JAR**

### 方法 B：完整關閉（釋放資源）

```powershell
cd "d:\Kapua_cms_bill_1.6.12\kapua-1.6.12\kapua-1.6.12\deployment\docker\compose"
docker compose down
```

- 容器被刪除，下次啟動需執行 `docker compose up -d` 並**重新部署自訂 JAR**

---

## 常見問題

### kapua-api 無回應（502 Bad Gateway）

```powershell
# 查看 API 日誌
docker logs kapua-api --tail 50

# 若 JAR 沒有載入，重新部署
docker cp "d:\Kapua_cms_bill_1.6.12\kapua-api-resources-running.jar" `
    kapua-api:/var/opt/jetty/webapps/root/WEB-INF/lib/kapua-rest-api-resources-1.6.12.jar
docker restart kapua-api
```

### 某個容器沒有啟動

```powershell
# 查看該容器日誌
docker logs <容器名稱> --tail 100

# 單獨重啟某個容器
docker restart <容器名稱>
```

### hosts 設定失效（cms.test 無法解析）

以系統管理員重新執行：
```powershell
d:\Kapua_cms_bill_1.6.12\tools\add-cms-test-host.ps1
```

### 查看 API 即時請求

打開瀏覽器前往 http://localhost:9999，可看到所有經過 debug-proxy 的 API 請求。

---

## 快速備忘

```powershell
# === 啟動（使用 stop 暫停過的）===
cd "d:\Kapua_cms_bill_1.6.12\kapua-1.6.12\kapua-1.6.12\deployment\docker\compose"
docker compose start

# === 啟動（使用 down 關閉過的）===
cd "d:\Kapua_cms_bill_1.6.12\kapua-1.6.12\kapua-1.6.12\deployment\docker\compose"
$env:IMAGE_VERSION = "1.6.12"
docker compose up -d
# 等 30 秒後...
docker cp "d:\Kapua_cms_bill_1.6.12\kapua-api-resources-running.jar" kapua-api:/var/opt/jetty/webapps/root/WEB-INF/lib/kapua-rest-api-resources-1.6.12.jar
docker restart kapua-api

# === 關機（保留容器）===
docker compose stop

# === 確認狀態 ===
docker ps --format "table {{.Names}}\t{{.Status}}"
```
